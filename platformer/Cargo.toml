[package]
name = "platformer"
version = "1.0.0"
edition = "2021"

[dependencies]
turbo = { version = "4.0.0e = "turbo-genesis-sdk" }

[lib]
crate-type = ["cdylib"]

[lints.rust]
unexpected_cfgs = { level = "allow" }
